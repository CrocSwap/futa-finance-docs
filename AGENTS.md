# Repository instructions

- Do not create git commits or push on the user's behalf; leave changes in the
  working tree for the user to commit.
- Everything under `website/` is synced from upstream and overwritten on each
  sync; do not edit it here. Repo-local files are `README.md`, `AGENTS.md`,
  and `.gitignore`.
